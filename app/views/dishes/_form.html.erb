<%= form_for(@dish) do |f| %>
  <% if @dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% @dish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="">
    <%= f.label :name %>
    <%= f.text_field :name %>
    </br>
    <%= f.label :introduction %>
    <%= f.text_field :introduction %>
    </br>
    <%= f.label :price %>
    <%= f.text_field :price %>
    </br>
    <%= f.label :availability %>
    <%= f.check_box :availability %>
    </br>
    <%= f.label :catalog %>
    <%= f.select :catalog_id, @catalogs.collect{|p| [p.description, p.id]} %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
