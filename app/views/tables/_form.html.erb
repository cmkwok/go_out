<%= form_for(@table) do |f| %>
  <% if @table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% @table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :min_capacity %>
    <%= f.text_field :min_capacity %>
    </br>
    <%= f.label :max_capacity %>
    <%= f.text_field :max_capacity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
