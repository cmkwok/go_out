<%= form_for(@salary_rate) do |f| %>
  <% if @salary_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salary_rate.errors.count, "error") %> prohibited this salary_rate from being saved:</h2>

      <ul>
      <% @salary_rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
