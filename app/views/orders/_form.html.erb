<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :number_customer %>
    <%= f.text_field :number_customer %>
    </br>
    <%= f.label :number_customer %>
    <%= f.text_field :number_customer %>
    </br>
    <%= f.label :table %>
    <%= f.select :table_id, @tables.collect{|p| ["#{p.description} #{p.max_capacity} people", p.id]} %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
